<div class="navbar">
  <%= button_to "home", root_path, class: "nav_button", method: :get %>
  <%= button_to "memory collection", make_memory_path, class: "nav_button", method: :get %>
  <%= button_to "record a memory", new_memory_path, class: "nav_button", method: :get %>
</div>

<h1 class="main_title">your memories.</h1>

<div class="main_content">
  <table>
    <div class="memory_block">
      <% @memory.each do |memory| %>
        <tr>
          <td><%= memory.title %></td>
        </tr>
        <tr>
          <td><%= memory.description %></td>
        </tr>
        <tr class="align_element">
          <td><%= button_to 'edit', edit_memory_path(memory), class: "redirect_button", method: :get %></td>
          <td><%= button_to 'show', memory_path(memory), class: "redirect_button", method: :get %></td>
          <td><%= button_to 'delete', memory_path(memory), class: "delete_button", method: :delete,
	    data: {confirm: "This will permanently delete this memory. Continue?"} %></td>
        </tr>
	<% end %>
    </div>
  </table>
</div>
